{
  "name": "parcel-transformer-pandoc",
  "version": "0.1.0",
  "license": "GPL3",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "npm run build",
    "build": "esbuild src/index.mjs --bundle --platform=node --outdir=lib"
  },
  "dependencies": {
    "@parcel/plugin": "^2.9.3",
    "minimatch": "^9.0.3",
    "omit": "^1.0.1",
    "tmp": "^0.2.1"
  },
  "engines": {
    "node": ">=12",
    "parcel": "^2.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.18.15"
  }
}
